\documentclass[11pt, a4paper]{report}

\usepackage{amsmath}
\usepackage{bm}
\usepackage[a4paper, margin=0.5in]{geometry}
\usepackage{amsfonts}
\usepackage{mathtools}
\usepackage{tikz}
\usetikzlibrary{automata, positioning}

\usepackage{pgfplots}
\pgfplotsset{width=5cm,compat=1.9}

% remove paragraph indent
\setlength{\parindent}{0em}

% hyperlinks for the table of contents
% \usepackage{color}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true, % make the links colored
    linkcolor=blue, % color TOC links in blue
    urlcolor=red, % color URLs in red
    linktoc=all, % 'all' will create links for everything in the TOC
    bookmarksnumbered=true
}

% to insert images
\usepackage{graphicx}
\graphicspath{ {./images/} {../images/} {../../images/}}

\usepackage{subfiles}

\DeclareMathOperator*{\minimize}{minimize}
\DeclareMathOperator*{\maximize}{maximize}
\DeclareMathOperator*{\argmin}{argmin}
\DeclareMathOperator*{\argmax}{argmax}

\title{Probability Notes}

\begin{document}
    \maketitle{}
    \tableofcontents
    \cleardoublepage
    \pdfbookmark{\contentsname}{toc}
    % \newpage

    \pagenumbering{arabic}
    \subfile{sections/probability_theorems}
    \subfile{sections/iterated_exp}

    %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    \chapter{Random number of Random Variables}
    Let $X_{i}$ be independent identically distributed Random Variables and let $Y = \sum_{i=1}^{N} X_{i}$ be the sum of $N$ such random variables where $N$ itself is a random variable. Then,
    \begin{align*}
        Y &= X_{1} + X_{2} + \cdots + X_{N}\\
        E[Y|N=n] &= \sum_{i=1}^{n}E[X_{i}]\\
                &= NE[X]\\
        E[Y] &= E[E[Y|N]] = E[NE[X]]\\
            &= E[N]E[X] \tag*{since $E[X]$ will be a number}\\
        Var(Y) &= E[Var(Y|N)] + Var(E[Y|N])\\
            &= E[NVar(X)] + Var(NE[X])\\
            &= E[N]Var(X) + E[X]^{2}Var(N)
    \end{align*}

    \subfile{sections/convolution}
    \subfile{sections/moment_gen}
    \subfile{sections/probability_distributions}
    \subfile{sections/markov_process}
    \subfile{sections/clt}
    \subfile{sections/sample_mean_var}
    \subfile{sections/parameter_estimation}
    \subfile{sections/hypothesis_testing}
    \subfile{sections/linear_regression}
    \subfile{sections/life_testing}
    \subfile{sections/simulation}

    %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    \chapter{Exercises}
    \subfile{sections/problems}
    \subfile{sections/solutions}
\end{document}
